# Sequential Thinking MCP — 项目部署手册

> **版本**: v1.2.1  
> **项目类型**: 纯 Python MCP 服务  
> **传输协议**: Streamable HTTP + stdio  
> **默认端口**: 20010  
> **MCP 工具数**: 26 个  
> **MCP Resources**: 2 个  
> **MCP Prompts**: 3 个  
> **依赖管理**: UV  
> **最低 Python**: 3.11+  
> **许可证**: MIT  
> **GitHub**: https://github.com/LseKit/SequentialThinking

---

## 一、项目概述

### 一句话

SequentialThinking 是一个结构化多步推理 MCP 服务，提供双引擎（LLM + 启发式）思维链推理。

### 核心特性

| 特性 | 说明 |
|------|------|
| **双引擎** | LLM 引擎（DeepSeek）+ 启发式引擎（全功能、零假数据），LLM 失败自动降级 |
| **Self-MoA** | 多轮独立 LLM 采样（不同温度），投票综合——结果更稳定 |
| **双协议** | Streamable HTTP（对内网 MCPHub）+ stdio（本地客户端） |
| **26 个工具** | 会话管理 8 + 质控 3 + 管理 4 + 增强 11 |
| **9 种模板** | 科学方法、五问法、根因分析、决策矩阵、SWOT、利弊分析、第一性原理、鱼骨图、帕累托 |
| **FTS5 全文搜索** | SQLite FTS5，替代 LIKE，高效检索 |
| **自动完成** | conclusion + quality > 0.7 自动标记会话 |
| **假设追踪** | LLM 提取隐含假设，支持验证/推翻/追踪 |
| **修订标记** | is_revision + revises_step |
| **交叉验证** | LLM ↔ 启发式双评质量分，分歧 > 0.2 自动标记 |
| **SQLite WAL** | 异步持久化，FK 外键约束，级联删除 |
| **182 个测试** | 8 个模块全覆盖 |

---

## 二、部署后的完整目录结构

> ⚠️ 下面展示的是**部署完成后**的完整目录。标注「✅ 源码自带」的是 GitHub 仓库里就有的文件；标注「🔧 需要你创建」的是部署时要自己建的。

```
/mcp/SequentialThinking/                    ← 项目根目录
│
├── Config/                                 ← 🔧 需要你创建（mkdir）
│   └── .env                                ← 🔧 需要你创建（chmod 600）
│                                             敏感凭据：DEEPSEEK_API_KEY
│                                             模板：复制 GitHubSrc/.env.example
│
├── SeqThinkVenV/                            ← 🔧 UV 自动创建（uv venv）
│   └── bin/python                           ← 项目专属 Python 解释器
│
├── Ecosystem/                               ← 🔧 需要你创建（mkdir）
│   └── ecosystem.config.js                  ← 🔧 需要你创建
│                                             PM2 配置：非敏感参数 + 进程管理
│                                             模板：见附录 A.2
│
├── GitHubSrc/                               ← ✅ 源码自带（git clone）
│   ├── __init__.py                          # 包入口，版本号
│   ├── __main__.py                          # 启动入口，CLI 参数解析
│   ├── Requirements.txt                     # Python 依赖清单
│   ├── .env.example                         # 环境变量模板（敏感值用 **** 替代）
│   ├── ecosystem.config.example.js          # PM2 配置模板（参考附录）
│   ├── .gitignore                           # Git 忽略规则
│   ├── 项目部署手册.md                       # 本文件
│   │
│   ├── Core/                                # 数据模型层
│   │   ├── Models.py                        # 16 个 dataclass
│   │   ├── Templates.py                     # 9 种思维模板
│   │   └── Context.py                       # 全局引用
│   │
│   ├── Engine/                              # 推理引擎层
│   │   └── Engine.py                        # HeuristicEngine + LLMEngine + Self-MoA
│   │
│   ├── Storage/                             # 持久化层
│   │   └── Store.py                         # aiosqlite WAL + FTS5 + FK 级联删除
│   │
│   ├── Handlers/                            # MCP 工具层（26 个）
│   │   ├── Register.py                      # 工具注册入口
│   │   ├── Helpers.py                       # _dispatch() 统一调度
│   │   ├── SessionHandlers.py               # 8 个会话管理工具
│   │   ├── QualityHandlers.py               # 3 个质控工具
│   │   ├── AdminHandlers.py                 # 4 个管理工具
│   │   └── EnhanceHandlers.py               # 11 个增强工具
│   │
│   ├── Server/                              # 服务入口层
│   │   └── Main.py                          # FastMCP + lifespan + Resources + Prompts
│   │
│   ├── Utils/                               # 工具层
│   │   └── Logger.py                        # stdout 日志
│   │
│   └── Tests/                               # 182 个测试
│       ├── pytest.ini                       # pytest 配置
│       ├── TestCoreModels.py                # 数据模型（31）
│       ├── TestCoreTemplates.py             # 模板（13）
│       ├── TestCoreContext.py               # 上下文（4）
│       ├── TestUtilsLogger.py               # 日志（6）
│       ├── TestHandlersHelpers.py           # 辅助函数（24）
│       ├── TestHeuristicEngine.py           # 启发式引擎（26）
│       ├── TestLlmEngine.py                 # LLM 引擎（30）
│       └── TestStorageStore.py              # 持久化（51）
│
├── Data/                                    ← 🔧 运行时自动创建（无需手动建）
│   └── sequential-thinking.db               ← SQLite 数据库（WAL 模式）
│
└── Logs/                                    ← 🔧 PM2 自动创建（无需手动建）
    ├── SequentialThinking-out.log
    └── SequentialThinking-err.log
```

### 你需要创建的文件和目录清单

| # | 创建什么 | 命令 | 模板/参考 |
|---|---------|------|----------|
|| 1 | `Config/` 目录 | `mkdir -p Config` | — |
|| 2 | `Config/.env` | `cp GitHubSrc/.env.example Config/.env` | [敏感凭据模板](/mcp/SequentialThinking/GitHubSrc/.env.example) |
|| 3 | `Config/.env` 权限 | `chmod 600 Config/.env` | 仅 owner 可读写 |
|| 4 | `Ecosystem/` 目录 | `mkdir -p Ecosystem` | — |
|| 5 | `Ecosystem/ecosystem.config.js` | 手动创建 | [PM2配置模板](/mcp/SequentialThinking/GitHubSrc/ecosystem.config.example.js) |
|| 6 | `SeqThinkVenV/` 虚拟环境 | `uv venv SeqThinkVenV --python 3.12` | UV 自动创建 |
|| 7 | `Data/` 和 `Logs/` | 无需手动创建，运行时自动生成 | — |

---

## 三、从零部署步骤

### 前提

- Python 3.12（建议用 UV 管理）
- UV（`curl -LsSf https://astral.sh/uv/install.sh | sh`）
- PM2（`npm i -g pm2`）

### 步骤 1：克隆源码并建立项目目录

```bash
# 创建项目根目录
mkdir -p /mcp/SequentialThinking

# 克隆源码到 GitHubSrc/
git clone <仓库地址> /mcp/SequentialThinking/GitHubSrc

# 进入项目根
cd /mcp/SequentialThinking
```

### 步骤 2：创建 UV 虚拟环境

```bash
uv venv SeqThinkVenV --python 3.12
```
- 目录名 `SeqThinkVenV` = Sequential Thinking Virtual Environment（PascalCase）
- 路径：`/mcp/SequentialThinking/SeqThinkVenV/`
- 不污染系统 Python

### 步骤 3：安装 Python 依赖

```bash
uv pip install -r GitHubSrc/Requirements.txt
```

### 步骤 4：创建配置目录和凭据文件

```bash
mkdir -p Config
cp GitHubSrc/.env.example Config/.env
chmod 600 Config/.env
```

然后编辑 `Config/.env`，填入真实值（详见下方「.env 参数详解」）。

### 步骤 5：创建 PM2 配置文件

```bash
mkdir -p Ecosystem
```

创建 `Ecosystem/ecosystem.config.js`，内容见附录 A.2。

### 步骤 6：启动服务

```bash
pm2 start Ecosystem/ecosystem.config.js
pm2 save
```

PM2 实际执行的命令：
```
SeqThinkVenV/bin/python GitHubSrc/__main__.py --env-file Config/.env
```

### 步骤 7：验证

```bash
# 检查 PM2 进程
pm2 status SequentialThinking

# 检查端口
ss -tlnp | grep 20010

# 跑测试
cd GitHubSrc
python3 -m pytest Tests/ -q
```

---

## 四、完整技术栈图

```
┌──────────────────────────────────────────────────────────────────────────┐
│                        PM2 (SequentialThinking)                          │
│  Ecosystem/ecosystem.config.js                                           │
│  ┌──────────────────────────────────────────────────────────────────┐    │
│  │ interpreter: "SeqThinkVenV/bin/python"  ← UV 虚拟环境 Python     │    │
│  │ script:      "GitHubSrc/__main__.py"    ← Python 入口文件        │    │
│  │ args:        "--env-file Config/.env"    ← .env 路径参数化       │    │
│  │                                                                   │    │
│  │ env:                                                              │    │
│  │   ST_TRANSPORT = http          ← 传输协议                         │    │
│  │   ST_HOST      = 0.0.0.0       ← 监听地址（对内网开放）            │    │
│  │   ST_PORT      = 20010         ← 监听端口                         │    │
│  │   ST_DB_PATH   = Data/...      ← 数据库路径                       │    │
│  │   ST_LLM_MODEL = deepseek-...  ← LLM 模型名                       │    │
│  │   ST_LLM_TIMEOUT = 120         ← API 超时                         │    │
│  │   PYTHONUNBUFFERED = 1         ← 日志实时输出                      │    │
│  └──────────────────────────────────────────────────────────────────┘    │
└──────────────────────────────┬───────────────────────────────────────────┘
                               │ spawn
                               ▼
┌──────────────────────────────────────────────────────────────────────────┐
│  SeqThinkVenV/bin/python GitHubSrc/__main__.py --env-file Config/.env    │
│                                                                          │
│  ┌─ __main__.py ────────────────────────────────────────────────────┐    │
│  │  sys.path → 项目根（/mcp/SequentialThinking/）                    │    │
│  │  __package__ = "GitHubSrc"  ← 建立包上下文，25处相对导入全通      │    │
│  │  argparse 解析 --env-file → SERVER_CONFIG["env_path"]             │    │
│  └──────────────────────────┬────────────────────────────────────────┘    │
│                             │                                             │
│                             ▼                                             │
│  ┌─ Server/Main.py._init() ──────────────────────────────────────────┐   │
│  │  load_dotenv("Config/.env")  ← 从 SERVER_CONFIG["env_path"] 加载  │   │
│  │  → os.environ["DEEPSEEK_API_KEY"] = "sk-xxx"                      │   │
│  │                                                                     │   │
│  │  Context.engine_ref = LLMEngine(api_key)  或  HeuristicEngine()   │   │
│  │  Context.store_ref  = Store(db_path)       ← SQLite WAL           │   │
│  └──────────────────────────┬─────────────────────────────────────────┘   │
│                             │                                             │
│                             ▼                                             │
│  ┌──────────────────────────────────────────────────────────────────┐    │
│  │                FastMCP 服务  端口 20010                           │    │
│  │                                                                   │    │
│  │  传输层:  Streamable HTTP  ← MCPHub / 内网客户端                  │    │
│  │           stdio           ← 本地 CLI                              │    │
│  │                                                                   │    │
│  │  ┌─────────────┐  ┌──────────────────┐                           │    │
│  │  │ LLMEngine   │  │ HeuristicEngine  │                           │    │
│  │  │ DeepSeek v4 │  │ 13 方法全真实数据 │                           │    │
│  │  │ httpx       │  │ 零外部依赖       │                           │    │
│  │  │ Semaphore 5 │  │ 自动降级兜底     │                           │    │
│  │  │ Self-MoA    │  └──────────────────┘                           │    │
│  │  └──────┬──────┘                                                 │    │
│  │         │                                                        │    │
│  │         ▼                                                        │    │
│  │   26 MCP 工具 + 2 Resources + 3 Prompts                          │    │
│  │                                                                   │    │
│  │  ┌─────────────────────┐                                         │    │
│  │  │ SQLite (WAL 模式)   │                                         │    │
│  │  │ 6 表 + FTS5 全文搜索│                                         │    │
│  │  │ FK 外键 + 级联删除  │                                         │    │
│  │  │ Data/sequential-    │                                         │    │
│  │  │ thinking.db         │                                         │    │
│  │  └─────────────────────┘                                         │    │
│  └──────────────────────────────────────────────────────────────────┘    │
└──────────────────────────────────────────────────────────────────────────┘

                     ┌──────────────────────────────────┐
                     │     配置分离原则                  │
                     │                                  │
                     │  Config/.env                     │
                     │  ← DEEPSEEK_API_KEY（敏感）       │
                     │  ← chmod 600                     │
                     │                                  │
                     │  Ecosystem/ecosystem.config.js   │
                     │  ← env 字段：ST_PORT/ST_HOST...  │
                     │  ← 非敏感配置（可入 Git）         │
                     │                                  │
                     │  CLI 参数（--env-file）           │
                     │  ← .env 路径由命令行指定          │
                     └──────────────────────────────────┘
```

---

## 五、配置详解

### 5.1 配置流向

```
CLI --env-file Config/.env  ──→  load_dotenv()  ──→  os.environ["DEEPSEEK_API_KEY"]
                                                          │
ecosystem.config.js env:     ──→  PM2 自动注入进程  ──→  os.environ["ST_PORT"]
  ST_PORT / ST_HOST / ...                                     │
                                                          │
CLI --port / --host / ...    ──→  argparse 解析  ──→  SERVER_CONFIG["port"]
                                                          │
                                                    ┌─────▼──────┐
                                                    │ 代码默认值   │
                                                    │ (兜底)      │
                                                    └────────────┘

优先级: CLI 参数 > 环境变量 > 代码默认值
```

### 5.2 Config/.env 参数详解

这是部署时**你需要手动创建**的文件，模板：`GitHubSrc/.env.example`。

**只包含真正的敏感凭据。** 非敏感参数不在这个文件里。

| 参数名 | 必须？ | 说明 |
|--------|:-----:|------|
| `DEEPSEEK_API_KEY` | 建议 | **模型1** API 密钥。不填则降级为纯启发式引擎（所有工具仍可用，但 moa_analyze、extract_assumptions 等依赖 LLM 的工具不可用） |
| `DASHSCOPE_API_KEY` | 按需 | **模型2** API 密钥（DashScope 官方标准名）。仅当 `ST_LLM_SELECTOR=2` 或 `3` 时需要。获取: https://dashscope.console.aliyun.com/apiKey |

**示例：**
```bash
DEEPSEEK_API_KEY=****
DASHSCOPE_API_KEY=****
```

> 🔴 创建后执行 `chmod 600 Config/.env` 确保仅 owner 可读。

### 5.3 Ecosystem/ecosystem.config.js 参数详解

这是部署时**你需要手动创建**的文件。完整模板见附录 A.2。

#### 5.3.1 进程控制参数

| 参数 | 类型 | 说明 | 推荐值 |
|------|------|------|--------|
| `name` | string | PM2 进程名，`pm2 status` 时显示 | `"SequentialThinking"` |
| `cwd` | string | 工作目录。用 `__dirname` 自动指向 ecosystem.config.js 所在目录 | `__dirname` |
| `interpreter` | string | Python 解释器路径。指向 UV 虚拟环境中的 Python | `"SeqThinkVenV/bin/python"` |
| `script` | string | 启动脚本。指向 Python 入口文件 | `"GitHubSrc/__main__.py"` |
| `args` | string | 启动参数。`--env-file` 指定 .env 位置 | `"--env-file Config/.env"` |

#### 5.3.2 env 字段（非敏感环境变量）

`env` 字段中的所有变量会在 PM2 启动时自动注入进程。**这些不是敏感信息，可以写在 ecosystem 里。**

| 变量名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `PYTHONUNBUFFERED` | string | — | 设为 `"1"` 使 Python 日志实时输出，不被缓冲 |
| `ST_TRANSPORT` | string | `"http"` | 传输协议。`"http"` = Streamable HTTP（对内网 MCPHub），`"stdio"` = 标准输入输出（本地 CLI） |
| `ST_HOST` | string | `"0.0.0.0"` | HTTP 监听地址。`"0.0.0.0"` = 所有网卡（内网可访问），`"127.0.0.1"` = 仅本机 |
| `ST_PORT` | string | `"20010"` | HTTP 监听端口 |
| `ST_DB_PATH` | string | `"Data/sequential-thinking.db"` | SQLite 数据库路径。相对路径相对于项目根目录 |
| `ST_LLM_SELECTOR` | string | `"3"` | **模型选择开关：** `0`=启发式引擎 `1`=仅模型1 `2`=仅模型2 `3`=双模型（推荐） |
| `ST_LLM_API_BASE` | string | `"https://api.deepseek.com/v1"` | **模型1** API 端点 URL |
| `ST_LLM_MODEL` | string | `"deepseek-v4-flash"` | **模型1** 模型名称 |
| `ST_LLM2_API_BASE` | string | `""` | **模型2** API 端点 URL。ST_LLM_SELECTOR=2 或 3 时需要 |
| `ST_LLM2_MODEL` | string | `""` | **模型2** 模型名称。ST_LLM_SELECTOR=2 或 3 时需要 |
| `ST_LLM_MODE` | string | `"self-moa"` | **推理模式：** `self-moa`=温度采样（默认，适合日常推理）`iterative`=迭代推理（双模型互相看输出后修正，适合重要决策） |
| `ST_MOA_ROUNDS` | string | `"3"` | **推理深度（1~20）：** self-moa 模式=温度采样数，iterative 模式=迭代轮次。⚠️ 推荐 1~3，3+ 后边际收益急剧下降 |
| `ST_LLM_TIMEOUT` | string | `"120"` | API 单次调用超时秒数 |

---

### 5.5 作者当前部署方案（推荐参考）

> 以下展示的是作者（小逸）在生产环境的实际部署配置，供参考。

**服务器环境：**
| 项目 | 值 |
|------|-----|
| 服务器 | HPE (Xeon E-2236, 6C/12T, 31GB) |
| 系统 | Ubuntu 24.04 LTS |
| Python | 3.12.3（UV 虚拟环境 `SequentialThinkingVenV/`） |
| PM2 | 7.0.3 |
| 端口 | 20010（Streamable HTTP） |
| 数据库 | SQLite WAL @ `Data/sequential-thinking.db` |

**两份配置文件的作者实际值：**

`Config/.env`（仅敏感凭据）：
```bash
DEEPSEEK_API_KEY=sk-****
DASHSCOPE_API_KEY=sk-****
```

`Ecosystem/ecosystem.config.js` 的 env 字段：
```javascript
env: {
  ST_LLM_SELECTOR: "3",       // 双模型
  ST_LLM_MODEL: "deepseek-v4-flash",
  ST_LLM2_MODEL: "qwen3.7-plus",
  ST_LLM_MODE: "self-moa",    // 默认温度采样，必要时切 iterative
  ST_MOA_ROUNDS: "3",         // 质量与成本最佳平衡点
  ST_LLM_API_BASE: "https://api.deepseek.com/v1",
  ST_LLM2_API_BASE: "https://dashscope.aliyuncs.com/compatible-mode/v1",
  ST_HOST: "0.0.0.0",
  ST_PORT: "20010",
  ST_DB_PATH: "Data/sequential-thinking.db",
  ST_LLM_TIMEOUT: "120",
}
```

> 建议：新人直接复制此方案即可用。后续根据需要调整 `ST_LLM_MODE` 和 `ST_MOA_ROUNDS`。

#### 5.3.3 进程管理参数

| 参数 | 类型 | 说明 | 推荐值 |
|------|------|------|--------|
| `autorestart` | bool | 崩溃后自动重启 | `true` |
| `max_restarts` | int | 连续重启上限，超过则停止重启 | `10` |
| `restart_delay` | int | 重启间隔（毫秒） | `5000`（5 秒） |
| `min_uptime` | string | 至少运行多久才算成功启动 | `"10s"` |
| `max_memory_restart` | string | 内存超过此值自动重启 | `"500M"` |

#### 5.3.4 日志参数

| 参数 | 类型 | 说明 | 推荐值 |
|------|------|------|--------|
| `error_file` | string | stderr 日志路径 | `"Logs/SequentialThinking-err.log"` |
| `out_file` | string | stdout 日志路径 | `"Logs/SequentialThinking-out.log"` |
| `merge_logs` | bool | 合并 stdout 和 stderr | `true` |
| `log_date_format` | string | 日志时间戳格式 | `"YYYY-MM-DD HH:mm:ss Z"` |

### 5.4 CLI 参数详解

这些参数可以在命令行中覆盖环境变量和 ecosystem 配置。

| 参数 | 类型 | 覆盖谁 | 说明 |
|------|------|--------|------|
| `--env-file` | string | — | **.env 文件路径**。相对路径相对于项目根目录。例如 `--env-file Config/.env` |
| `--transport` | http/stdio | `ST_TRANSPORT` | 传输协议 |
| `--host` | string | `ST_HOST` | HTTP 监听地址 |
| `--port` | int | `ST_PORT` | HTTP 监听端口 |
| `--db-path` | string | `ST_DB_PATH` | 数据库路径 |

**使用示例：**
```bash
# 自定义 .env 位置
SeqThinkVenV/bin/python GitHubSrc/__main__.py --env-file /custom/path/.env

# stdio 模式
SeqThinkVenV/bin/python GitHubSrc/__main__.py --transport stdio

# 自定义端口和数据库
SeqThinkVenV/bin/python GitHubSrc/__main__.py --port 8080 --db-path /data/mydb.db
```

---

## 六、PM2 进程管理详解

### 6.1 为什么推荐 PM2

| 原因 | 说明 |
|:----|:------|
| **崩溃自动重启** | Python 进程意外退出时，PM2 自动拉起 |
| **内存超限自动重启** | `max_memory_restart: "1G"` 防止内存泄漏拖垮服务 |
| **日志统一管理** | stdout/stderr 自动写入文件，支持轮转（pm2-logrotate） |
| **开机自启** | `pm2 startup` + `pm2 save` 重启后自动恢复所有进程 |
| **进程守护** | 不会像 `nohup` 那样丢失进程，`pm2 status` 一眼看到所有状态 |

### 6.2 三种重启方式及适用场景

```
修改的内容                                   需要什么操作
────────────────────────────────────────────────────────────────
仅改 env: 里的值                              →  restart --update-env ✅
（如 ST_LLM_MODE、ST_MOA_ROUNDS、ST_PORT 等）
────────────────────────────────────────────────────────────────
改 script / cwd / interpreter / args          →  delete + start 🔴
（如换 Python 路径、改启动入口、改 CLI 参数）
────────────────────────────────────────────────────────────────
不改任何配置，仅清内存或重置状态               →  restart ✅
```

#### 场景一：只改 env 值

```bash
# 适用：ST_LLM_MODE self-moa → iterative
#       ST_MOA_ROUNDS 3 → 5
#       ST_PORT 20010 → 8080
#
# 🔑 PM2 用 --update-env 重新读取 ecosystem 的 env 字段
pm2 restart SequentialThinking --update-env
pm2 save
```

**为什么 `--update-env`？** 不加此参数时，PM2 重启进程时使用**进程创建时的旧环境变量快照**，不会读 ecosystem 的最新值。`--update-env` 强制重新解析 ecosystem 配置文件。

#### 场景二：改结构字段

```bash
# 适用：改 script / cwd / interpreter / args
#       新增环境变量名（第一次）
#       删除环境变量名
#
# 🔑 script / cwd 等字段在进程创建时固化，restart 不重新解析
pm2 delete SequentialThinking
pm2 start Ecosystem/ecosystem.config.js
pm2 save
```

**为什么必须 delete + start？** PM2 的 `restart` 本质是 `stop + start`，但它复用的是进程创建时缓存的一份配置快照。`script`、`cwd`、`interpreter` 以及 env 字段的**结构**（新增/删除变量名）在这份快照中固化，重启不会重新读取。只有 `delete` 删除整个进程记录后再 `start`，才会触发完整的配置解析。

> ⚠️ **常见误区**：很多用户改了 `ecosystem.config.js` 后只执行 `pm2 restart`，发现新配置没生效。原因即在此。

#### 场景三：不改配置仅重启

```bash
# 适用：清内存、重置状态、怀疑服务异常
#
# 🔑 最简单的重启，沿用当前所有配置和环境变量
pm2 restart SequentialThinking
```

### 6.3 启动、停止、查看状态

| 操作 | 命令 | 说明 |
|:-----|:-----|:-----|
| 启动 | `pm2 start Ecosystem/ecosystem.config.js` | 首次或 delete 后 |
| 停止 | `pm2 stop SequentialThinking` | 停止进程，保留记录 |
| 重启 | `pm2 restart SequentialThinking` | 沿用现有配置 |
| 重启（读新 env） | `pm2 restart SequentialThinking --update-env` | 重新加载 env 值 |
| 重建 | `pm2 delete SequentialThinking && pm2 start Ecosystem/ecosystem.config.js` | 加载全部新配置 |
| 保存 | `pm2 save` | 持久化进程列表 |
| 查看状态 | `pm2 status` | 检查 online / stopped / errored |
| 查看日志 | `pm2 logs SequentialThinking --lines 50` | 实时日志 |
| 开机自启 | `pm2 startup` | 系统重启后自动恢复 PM2 进程 |

### 6.4 Config/.env 修改后怎么处理

```bash
# 修改 Config/.env 后 → 必须重启进程
pm2 restart SequentialThinking
# 🔑 load_dotenv() 只在进程启动时执行一次，不重启不生效
```

不需要 `--update-env`，因为 `.env` 不是由 PM2 ecosystem 管理的，是 `__main__.py` 通过 `--env-file` 参数在 Python 进程内部加载。重启进程就会重新读。

---

## 七、MCP 工具清单

### 7.1 会话管理（8 个）

| 工具名 | 说明 |
|--------|------|
| `start_thinking` | 创建思维会话，9 种模板，返回复杂度评估 + 优化后问题 |
| `add_step` | 添加推理步骤（analysis/hypothesis/verification/conclusion），返回置信度 + 建议，**自动完成** |
| `update_step` | 更新已有步骤的内容或类型 |
| `review_thinking` | 获取完整思维链（linear/tree/summary） |
| `branch_thinking` | 从指定步骤创建替代推理分支 |
| `merge_insights` | 合并多个分支的洞察 |
| `delete_session` | 级联删除：会话 + 所有步骤/分支/标签 |
| `complete_session` | 手动标记会话为 completed |

### 7.2 质量控制（3 个）

| 工具名 | 说明 |
|--------|------|
| `validate_logic` | 检测逻辑谬误（无支撑结论、未验证假设） |
| `evaluate_quality` | 5 维质量评估（一致性/完整性/严谨性/创新性/可操作性）+ LLM↔启发式交叉验证 |
| `detect_biases` | 检测认知偏见（LLM 深度扫描 + 启发式规则双重） |

### 7.3 管理功能（4 个）

| 工具名 | 说明 |
|--------|------|
| `export_session` | 导出会话（Markdown/JSON/Text） |
| `list_sessions` | 列出会话，支持按状态和标签过滤 |
| `search_sessions` | FTS5 全文搜索历史会话 |
| `get_metrics` | 统计指标（day/week/month/all） |

### 7.4 增强功能（11 个）

| 工具名 | 说明 |
|--------|------|
| `compare_sessions` | 基于 Jaccard 相似度对比两会话 |
| `suggest_next` | 上下文感知的下一步建议 |
| `rewrite_query` | LLM 优化问题表述 |
| `visualize_thinking` | 导出 Mermaid 思维流程图 |
| `replay_thinking` | 逐步回放推理过程 |
| `auto_tag` | 自动生成标签 |
| `moa_analyze` | Self-MoA 多轮独立 LLM 采样投票 |
| `add_connection` | 步骤间添加双向推理链路 |
| `detect_contradictions` | 自相矛盾检测（启发式 + LLM） |
| `extract_assumptions` | LLM 提取隐含假设 |
| `update_tags` | 替换会话全部标签 |

### 7.5 MCP Resources（2 个）

| URI | 说明 |
|-----|------|
| `config://server` | 服务配置（名称、版本、工具数） |
| `thinking://templates/catalog` | 全部 9 种思维模板目录 |

### 7.6 MCP Prompts（3 个）

| 名称 | 说明 |
|------|------|
| `problem_breakdown` | 问题分解提示 |
| `critical_analysis` | 批判性分析提示 |
| `synthesis_prompt` | 综合结论提示 |

---

## 八、测试指南

### 8.1 运行测试

```bash
cd /mcp/SequentialThinking/GitHubSrc

# 全量（182 个）
python3 -m pytest Tests/ -v

# 只跑不依赖数据库的（跳过 TestStorageStore）
python3 -m pytest Tests/ -v --ignore=Tests/TestStorageStore.py

# 只跑某个模块
python3 -m pytest Tests/TestStorageStore.py -v
```

### 8.2 测试文件清单

| 文件 | 数量 | 覆盖内容 |
|------|:--:|------|
| `TestCoreModels.py` | 31 | 15 个 dataclass + 5 个常量 |
| `TestCoreTemplates.py` | 13 | 9 个模板 |
| `TestCoreContext.py` | 4 | 全局引用 |
| `TestUtilsLogger.py` | 6 | 日志工具 |
| `TestHandlersHelpers.py` | 24 | 辅助函数 |
| `TestHeuristicEngine.py` | 26 | 启发式引擎 13 方法 |
| `TestLlmEngine.py` | 30 | LLM 引擎 13 方法 + MoA（mock HTTP） |
| `TestStorageStore.py` | 51 | 持久化存储（真实 SQLite） |

---

## 九、运维管理

### 9.1 数据库备份与维护

```bash
# 备份数据库
cp Data/sequential-thinking.db Data/backup-$(date +%Y%m%d).db

# 压缩数据库（释放已删除数据占用的空间）
sqlite3 Data/sequential-thinking.db "VACUUM;"

# 查看数据库大小
ls -lh Data/sequential-thinking.db
```

> PM2 进程管理的完整说明见上文「六、PM2 进程管理详解」。

---

## 十、故障排查

| 问题 | 排查 |
|------|------|
| `ModuleNotFoundError` | 确认在项目根目录执行，虚拟环境已安装依赖 |
| `Address already in use` | 端口被占用，用 `--port` 更换或 `kill` 占用进程 |
| LLM 不工作 | 确认 `Config/.env` 中 `DEEPSEEK_API_KEY` 正确，确认 `--env-file` 指向正确路径 |
| 降级到启发式引擎 | 确认 API Key 有效、网络能访问 API 端点 |
| 测试全部失败 | 确认在 `GitHubSrc/` 目录下运行 |
| `ImportError: relative import beyond top-level` | 测试文件导入未用 `sys.path + GitHubSrc.` 前缀 |

---

## 附录 A：完整配置示例

### A.1 最小配置（仅凭据）

```bash
# Config/.env
DEEPSEEK_API_KEY=sk-xxx
```

```bash
cd /mcp/SequentialThinking
SeqThinkVenV/bin/python GitHubSrc/__main__.py --env-file Config/.env
```

### A.2 完整 Ecosystem/ecosystem.config.js

```javascript
/**
 * Sequential Thinking MCP — PM2 Ecosystem 配置
 *
 * 部署时将此文件放在: /mcp/SequentialThinking/Ecosystem/ecosystem.config.js
 *
 * PM2 实际执行: SeqThinkVenV/bin/python GitHubSrc/__main__.py --env-file Config/.env
 *
 * 启动命令: pm2 start Ecosystem/ecosystem.config.js
 */
module.exports = {
  apps: [{
    // ── 进程名 ──
    name: "SequentialThinking",

    // ── 工作目录（__dirname = Ecosystem/ 的父目录 = 项目根）──
    cwd: __dirname,

    // ── Python 解释器（UV 虚拟环境）──
    interpreter: "SeqThinkVenV/bin/python",

    // ── 入口文件 ──
    script: "GitHubSrc/__main__.py",

    // ── 启动参数：指定 .env 位置 ──
    args: "--env-file Config/.env",

    // ── 非敏感环境变量（PM2 自动注入进程）──
    env: {
      PYTHONUNBUFFERED: "1",           // Python 日志实时输出
      ST_TRANSPORT: "http",            // 传输协议: http 或 stdio
      ST_HOST: "0.0.0.0",             // 监听地址: 0.0.0.0=内网可访问
      ST_PORT: "20010",               // 监听端口
      ST_DB_PATH: "Data/sequential-thinking.db",  // 数据库路径
      ST_LLM_MODEL: "deepseek-v4-flash",  // 模型名
      ST_LLM_TIMEOUT: "120",          // API 超时秒数
    },

    // ── 日志 ──
    error_file: "Logs/SequentialThinking-err.log",
    out_file: "Logs/SequentialThinking-out.log",
    merge_logs: true,
    log_date_format: "YYYY-MM-DD HH:mm:ss Z",

    // ── 自动重启 ──
    autorestart: true,
    max_restarts: 10,
    restart_delay: 5000,
    min_uptime: "10s",
    max_memory_restart: "500M",
  }],
};
```

### A.3 stdio 模式（MCP 客户端直连）

```json
{
  "mcpServers": {
    "sequential-thinking": {
      "command": "/mcp/SequentialThinking/SeqThinkVenV/bin/python",
      "args": [
        "/mcp/SequentialThinking/GitHubSrc/__main__.py",
        "--env-file", "/mcp/SequentialThinking/Config/.env",
        "--transport", "stdio"
      ],
      "env": { "DEEPSEEK_API_KEY": "sk-your-key" }
    }
  }
}
```

### A.4 纯启发式引擎（无 LLM）

```bash
cd /mcp/SequentialThinking
uv venv SeqThinkVenV --python 3.12
uv pip install -r GitHubSrc/Requirements.txt
SeqThinkVenV/bin/python GitHubSrc/__main__.py
# 全部 26 个工具可用，HeuristicEngine 基于真实数据分析
# moa_analyze / extract_assumptions 需要 LLM，会返回提示
```

---

**文档版本**: v1.2.1  
**最后更新**: 2026-07-03  
**维护者**: 小逸 (LseKit)  
**GitHub**: https://github.com/LseKit/SequentialThinking
