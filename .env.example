# ═══════════════════════════════════════════════════════════
# Sequential Thinking MCP — 敏感凭据模板
# ═══════════════════════════════════════════════════════════
# 文件路径: GitHubSrc/.env.example
# 原作者: 小逸
# 官方仓库: https://github.com/LseKit/SequentialThinking
#
# 用途:
#     这是 .env 文件的模板，复制到项目根目录的 Config/.env 后填入真实值。
#     本文件可以进 Git 仓库（敏感值已用 **** 替代），
#     但 Config/.env 绝不能进 Git（包含真实 API Key）。
#
# 非敏感配置参见:
#     Ecosystem/ecosystem.config.js（端口、模型名、路径等）
#     GitHubSrc/ecosystem.config.example.js（模板）
#
# 部署命令:
#     cp GitHubSrc/.env.example Config/.env
#     chmod 600 Config/.env
#     编辑 Config/.env 填入真实 Key
# ═══════════════════════════════════════════════════════════

# ── 模型1：DeepSeek API Key ──
# 必填，不填则降级为启发式引擎。
# 获取地址: https://platform.deepseek.com/api_keys
# 官方 SDK 变量名: DEEPSEEK_API_KEY
DEEPSEEK_API_KEY=****

# ── 模型2：通义千问（DashScope）API Key ──
# ST_LLM_SELECTOR=2 或 3 时必须。
# 获取地址: https://dashscope.console.aliyun.com/apiKey
# 官方 SDK 变量名: DASHSCOPE_API_KEY（DashScope 官方标准）
DASHSCOPE_API_KEY=****
